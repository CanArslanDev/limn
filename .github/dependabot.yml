version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
      day: monday
    commit-message:
      prefix: "chore(ci)"
    labels:
      - dependencies
      - github-actions
    open-pull-requests-limit: 5

  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
      day: monday
    commit-message:
      prefix: "chore(deps)"
    labels:
      - dependencies
      - npm
    open-pull-requests-limit: 5
    ignore:
      # Bump provider SDKs deliberately, not weekly. Versions are pinned in
      # peerDependencies so a dependabot bump of devDependencies should not
      # silently shift our public surface.
      - dependency-name: "@anthropic-ai/sdk"
      - dependency-name: openai
    groups:
      build-tooling:
        patterns:
          - tsup
          - typescript
          - "@biomejs/*"
      test-tooling:
        patterns:
          - vitest
          - "@vitest/*"
      types:
        patterns:
          - "@types/*"
