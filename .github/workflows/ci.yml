name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: verify (Node ${{ matrix.node }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        node: ["20.10.0", "22.x"]
    steps:
      # Action major versions bumped to ones whose runners support Node 24
      # natively, ahead of the 2026-06-02 default flip that retires the
      # Node 20 actions runtime. checkout/setup-node moved to v6; pnpm/action-setup
      # moved to v5. Workflow Node matrix below is independent of the runner Node.
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 9.12.0
          run_install: false

      - name: Set up Node ${{ matrix.node }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm run lint

      - name: Typecheck
        run: pnpm run typecheck

      - name: Test (with coverage)
        run: pnpm run test:coverage

      - name: Upload coverage to Codecov
        if: matrix.node == '20.10.0'
        uses: codecov/codecov-action@v6
        with:
          files: coverage/lcov.info
          flags: unittests
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

      - name: Build
        run: pnpm run build

      - name: Verify package can be packed
        run: pnpm pack --pack-destination /tmp/limn-pack
